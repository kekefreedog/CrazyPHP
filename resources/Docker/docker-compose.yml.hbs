###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: '3.1'
services:
    
    # NGINX
    webserver:
        image: 'nginx:alpine'
        restart: always
        working_dir: '{{app.root}}' # /Users/kzarshenas/Sites/CrazyTest
        env_file:
            - './docker/variables.env'
        volumes:
            - '.:{{app.root}}' # '.:/Users/kzarshenas/Sites/CrazyTest'
            - './docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf'
            # If {{app.framework.path}} - './vendor/kzarshenas/crazyphp:/Users/kzarshenas/Sites/CrazyPHP'
        ports:
            - '48000:80'

    # PHP
    php-fpm:
        build: phpdocker/php-fpm
        restart: always
        working_dir: '{{app.root}}' # /Users/kzarshenas/Sites/CrazyTest
        env_file:
            - './docker/variables.env'
        volumes:
            - '.:{{app.root}}' # '.:/Users/kzarshenas/Sites/CrazyTest'
            - './docker/php-fpm/php-ini-overrides.ini:/etc/php/8.1/fpm/conf.d/99-overrides.ini'
            # Dev
            # If {app.framework.path} - './vendor/kzarshenas/crazyphp:/Users/kzarshenas/Sites/CrazyPHP'
            {{#if app.framework.path}}'./vendor/kzarshenas/crazyphp:{{app.framework.path}}'{{/if}}

    # MANGO
    mongo:
        image: mongo
        restart: always
        env_file:
            - './docker/variables.env'
        ports:
            - "27017:27017"

