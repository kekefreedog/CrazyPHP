FROM phpdockerio/php:8.2-fpm
WORKDIR ${PWD}
RUN apt-get update; \
    apt-get -y --no-install-recommends install \
        git \ 
{{#if _config.Database.collection.mongodb}}        php8.2-mongodb; \
{{/if}}    apt-get clean; \
    rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* /usr/share/doc/*