FROM phpdockerio/php:8.3-fpm
WORKDIR ${PWD}
RUN apt-get update; \
    apt-get -y --no-install-recommends install \ {{#if _config.App.dependencies.php.packages}}{{#each _config.App.dependencies.php.packages}}
        {{this}} \ {{/each}}{{else}}
        git \ {{/if}}
    apt-get clean; \
    rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* /usr/share/doc/*